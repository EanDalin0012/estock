<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.estock.stockmanagement.dao.importstock.ImportStockDAO">
    
    <select id="countImportStock" resultType="java.lang.Integer">
        <![CDATA[
			select coalesce(MAX(id), 0) FROM  import_stock;
	   ]]>
    </select>
    
    <insert id="addNewStockName">
        <![CDATA[
           INSERT INTO import_stock
			(
				id
				, represent_product_type_id
				, qty
				, total_price
				, "desc"
				, user_id
				, create_date
				, create_by
				, modify_date
				, modify_by
				, status
			)
			VALUES(
				#{id}
				, #{representProductTypeId}
				, #{totalQty}
				, #{totalPrice}
				, #{desc}
				, #{userId}
				, #{dateTime}
				, #{userId}
				, #{dateTime}
				, #{userId}
				, #{stutas}
			);
        ]]>
    </insert>
    
    <update id="deleteStockName">
    	 <![CDATA[
            UPDATE product
			SET 
				name= #{productName}
				, "desc"= #{desc}
				, modify_by=#{userId}
				, modify_date=#{dateTime}
				, status=#{stutas}
				, resource_id=#{resourceId}
			WHERE id = #{id};
        ]]>
    </update>
    
</mapper>