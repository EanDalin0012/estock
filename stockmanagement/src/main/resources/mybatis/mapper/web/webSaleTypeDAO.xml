<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.estock.stockmanagement.dao.web.WebSaleTypeDAO">

    <resultMap type="WebSaleTypeDTO" id="query">
        <id property="id" column="id"/>
        <result property="id" column="id"/>
        <result property="name" column="name"/>
    </resultMap>

    <select id="inquiry" resultMap="query">
        <![CDATA[
			SELECT
				 id
				, name
			FROM user_auth;
	   ]]>
    </select>
    
    <insert id="insert">
        <![CDATA[
            INSERT INTO public.sale_type
			(id, name, "desc", create_on, create_by, update_on, update_by)
			VALUES(0, '', '', '', 0, '', 0);

        ]]>
    </insert>
    
    <update id="udpate">
    	 <![CDATA[
            UPDATE sale_type
			SET 
				"name"		= #{name};
				, "desc"	= #{desc}
				, update_on	= #{updateOn}
				, update_by	=  #{userId}
			WHERE id		= #{id};
        ]]>
    </update>

</mapper>