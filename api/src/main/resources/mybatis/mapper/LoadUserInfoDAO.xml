<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.estock.api.dao.LoadUserInfoDAO">

    <resultMap type="LoadUserInfoDTO" id="loadUserInfoDTO">
        <id property="id" column="id"/>
        <result property="firstName" column="first_name"/>
        <result property="lastName" column="last_name"/>
        <result property="dateBirth" column="date_birth"/>
        <result property="gender" column="gender"/>
        <result property="phone" column="desc"/>
        <result property="desc" column="desc"/>
        <result property="dateTime" column="modify_date"/>
        <result property="userID" column="modify_by"/>
        <association property="authorities" resultMap="authorities" javaType="list"/>
        <association property="userRole" resultMap="userRoleDTO" javaType="UserRoleDTO"/>
        <association property="credential" resultMap="credential" javaType="CredentialDTO"/>
    </resultMap>

    <resultMap type="AuthorityDTO" id="authorities">
        <id property="id" column="authority_id"/>
        <result property="name" column="authority_name"/>
    </resultMap>

    <resultMap type="UserRoleDTO" id="userRoleDTO">
        <id property="id" column="role_id"/>
        <result property="role" column="role"/>
    </resultMap>

    <resultMap type="CredentialDTO" id="credential">
        <id property="id" column="role_id"/>
        <result property="userName" column="role"/>
        <result property="accountExpired" column="role"/>
        <result property="accountLocked" column="role"/>
        <result property="credentialsExpired" column="role"/>
        <result property="enabled" column="role"/>
    </resultMap>

    <select id="loadUserInfo" resultMap="loadUserInfoDTO">
        SELECT
            u.id
             , u.user_name
             , u."password"
             , u.account_expired
             , u.account_locked
             , u.credentials_expired
             , u.enabled
             , u.user_info_id
             , aut."name" as "authority_name"
             , aut."id" as "authority_id"
             , us_info.first_name
             , us_info.last_name
             , us_info.gender
             , us_info.date_birth
             , us_info.phone
             , us_info."desc"
             , us_info.status
             , us_info.resource_id
             , u_role."id" as "role_id"
             , u_role."role" as "role"
        FROM role_authority r_aut
                 INNER JOIN user_ u on u.role_id = r_aut.role_id
                 INNER JOIN authority aut on aut."id" = r_aut.authority_id
                 INNER JOIN user_info us_info on us_info.id = u.user_info_id
                 INNER JOIN user_role u_role on u_role."id" = u.role_id
        WHERE u.user_name = 'admin'
        UNION
        SELECT
            u.id
             , u.user_name
             , u."password"
             , u.account_expired
             , u.account_locked
             , u.credentials_expired
             , u.enabled
             , u.user_info_id
             , au."name" as "authority_name"
             , au."id" as "authority_id"
             , us_info.first_name
             , us_info.last_name
             , us_info.gender
             , us_info.date_birth
             , us_info.phone
             , us_info."desc"
             , us_info.status
             , us_info.resource_id
             , u_role."id" as "role_id"
             , u_role."role" as "role"
        FROM user_ as u
                 INNER JOIN users_authorities ua on u."id" = ua.user_id
                 INNER JOIN authority au on au."id" = ua.authority_id
                 INNER JOIN user_info us_info on us_info.id = u.user_info_id
                 INNER JOIN user_role u_role on u_role."id" = u.role_id
        WHERE user_name = 'admin'
        ORDER BY authority_id ASC;
    </select>

</mapper>